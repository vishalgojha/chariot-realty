# Vite exposes only variables prefixed with VITE_
VITE_API_BASE_URL=

# Backend (Supabase Postgres)
DATABASE_URL=

# Required for /api/ingest
INGEST_API_KEY=

# Railway provides PORT at runtime; no need to set locally unless desired.
# PORT=4173
